name: My Github Actions Worflow
on: [push]

jobs:
  testing_github: 
    runs-on: ubuntu-latest
    steps: 
      - name: Hello
        run: echo "Hello, Github Actions"
      - name: Display repo name
        run: echo "this workflow is running in ${{ github.repository}}"